name: ELIOS Autocode

on:
  workflow_dispatch: {}
  push:
    branches: [ "main" ]
    paths:
      - "scripts/autocode.js"          # si tu modifies le script
      - "scripts/scripts/clés/**"      # tes clés actuelles (avec accent)
      - "scripts/scripts/cles/**"      # variante sans accent
      - "clés/**"                      # si tu déplaces plus tard

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Générer un fichier
        run: node scripts/autocode.js

      - name: Commit & push
        run: |
          git config user.name "elios-bot"
          git config user.email "elios-bot@users.noreply.github.com"
          git add generated/
          git status
          git commit -m "ELIOS: autocode" || echo "⚠️ Aucun changement à committer"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
